# ─────────────────────────────────────────────────────────────
# DDoS Monitor — Backend Environment Variables
# Copy this file to .env and fill in real values.
# NEVER commit .env to git.
# ─────────────────────────────────────────────────────────────

# Runtime environment: dev | prod
ENV=dev

# Comma-separated list of allowed frontend origins (CORS)
# dev: http://localhost:5173
# prod: https://nitishyadav404.github.io
ALLOWED_ORIGINS=http://localhost:5173

# Secret key for internal-only endpoints (/api/ingest, /api/predict)
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
INTERNAL_INGEST_KEY=change-me-generate-a-long-random-string

# ─── Data Sources (fill when you create accounts) ────────────
ABUSEIPDB_API_KEY=
CLOUDFLARE_API_KEY=

# ─── Storage (fill when you create Supabase + Upstash) ───────
# Supabase Postgres connection string
DATABASE_URL=

# Upstash Redis REST URL
REDIS_URL=

# ─── Demo Mode ───────────────────────────────────────────────
# Set to true to serve pre-recorded attack data instead of live
DEMO_MODE=false
